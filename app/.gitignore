# Built application files
*.apk
*.aab
*.ap_

# Build files
/build
*.iml
.gradle

# Local config (contains your PC's paths — should never be shared)
local.properties

# IDE files
/.idea
*.iml

# Firebase configuration
# google-services.json must be placed in the app/ folder and is excluded from git.
google-services.json

# Keystore files (keep these secret!)
*.jks
*.keystore